import React from "react";
import ShapeContainer from "../features/shapmover/shapeContainer";

type Props = {};

const geometryPage: React.FC = ({}: Props) => {
  return <ShapeContainer />;
};

export default geometryPage;
